#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/** 
 * list_t - will be the variable that will be used to add a node at the end of he list 
 *
 *
 */
list_t *add_node_end(list_t **head, const char *str);

int main(void)
{
	list_t **head;

	head = NULL:

	add_node_end(%list_t);

	list_t(head);

	return (0);
}
