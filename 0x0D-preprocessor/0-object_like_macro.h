#ifndef OBJECT_LIKE_MACRO_H
#define OBJECT_LIKE_MACRO_H

/**
 * macros are include with the #define directive
 * the header file has been abbreviated with the token
 * betty will specify it requires Underscore not .h
 * the name of the file was included in the two direcives
 * the two directives are used to specify
 * the ifindef enures that the macro code hs nt been used before
 */


#define SIZE 1024

#endif
