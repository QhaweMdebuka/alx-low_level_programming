#include <stdio.h>

/**
 * _putchar - scripts the character o to standard output
 * @c: character to print
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set.
 */
int _putchar(char c)
{
	return (putchar(c));
}
