#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 *list_t *add_node is used to add a node at the begining of list_t
 *
 *
 */

list_t *add_node(list_t **head, const char *str);

int main(void)
{
	list_t *head;

	head = NULL;

	add_node(&head)

	return (0);
}
